export class Rating {
    /**
     * @param {string} source
     * @param {string} rating
     * @param {string} logo
     */
    constructor(source, rating, logo) {
        this.source = source;
        this.rating = rating;
        this.logo = logo;
    }
}