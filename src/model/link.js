export class Link {
    /**
     * @param {string} url
     * @param {string} name
     * @param {string} logo
     */
    constructor(url, name, logo){
        this.url = url;
        this.name = name;
        this.logo = logo;
    }
}